export default function Upload() {
    return (
        <div className="">
        <h1 className="text-black">Installation</h1>
        <p>
            To use the School_serve API, you will need to install the School_serve SDK. The SDK is available for several programming languages, including JavaScript, Python, and Ruby. You can install the SDK using a package manager or by downloading the source code from the School_serve GitHub repository.
        </p>
        <h2>JavaScript</h2>
        <p>
            To install the School_serve SDK for JavaScript, you can use npm or yarn. Run the following command in your project directory to install the SDK:
        </p>
        <pre>
            <code>
            npm install school_serve
            </code>
        </pre>
        <p>
            Alternatively, you can use yarn to install the SDK:
        </p>
        <pre>
            <code>
            yarn add school_serve
            </code>
        </pre>
        <h2>Python</h2>
        <p>
            To install the School_serve SDK for Python, you can use pip. Run the following command in your project directory to install the SDK:
        </p>
        <pre>
            <code>
            pip install school_serve
            </code>
        </pre>
        <h2>Ruby</h2>
        <p>
            To install the School_serve SDK for Ruby, you can use RubyGems. Run the following command in your project directory to install the SDK:
        </p>
        <pre>
            <code>
            gem install school_serve
            </code>
        </pre>
        </div>
    );
    }